# Estado Atual

Novo bloco iniciado para implementar a arquitetura UI/UX em `packages/vscode-extension`.

- Nova fundação entregue em `packages/vscode-extension`, mantida independente da extensão legada `extensions/idd-core`.
- Componentes: `IntentTreeDataProvider` por contexto/agregado/entidade/método; `ContractIndex` YAML local; `LspDiagnosticProvider` síncrono com diagnostics/quick fix para INV-UI-04; StatusBar HUD; `ContractDetailPanel.tsx` React/Tailwind.
- Validações concluídas: `npm run compile` e `npm run build:webview`. `tsconfig` também usa `forceConsistentCasingInFileNames`; o build gerou o bundle/estilo da webview e só há aviso não bloqueante de `caniuse-lite` desatualizado.
- Pendência de UI resolvida: `iddUi.openContract` cria `WebviewPanel` com CSP/nonce, bundle React e dados do contrato selecionado.
- Teste manual pronto: abrir `packages/vscode-extension` como pasta raiz e executar `Run IDD UI Sample` (F5); ele carrega `examples` com sidebar, painel de contrato e uma violação deliberada de INV-UI-04 em `UserAccount.ts`.
- PWA browser-first criada em `packages/idd-ui-pwa` para testar a UI sem Extension Development Host.
- Correção de build: `src/vite-env.d.ts` declara os tipos de `vite-plugin-pwa/client`, resolvendo `TS2307` para `virtual:pwa-register`.
- Validação: `npm run build` passou, gerando `dist/manifest.webmanifest`, `dist/sw.js` e precache offline.
- Ajuste de UX PWA: `App.tsx` apresenta intenção em linguagem natural, outcome e invariantes como bloco primário `01 Business intent`; o YAML é a projeção `COMPILED CONTRACT` secundária. `npm run build` passou após o ajuste.
- Padrões GitHub locais adicionados: `.editorconfig`, `.gitattributes`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `SUPPORT.md`, templates de Issue/PR, Dependabot e CI que executa builds dos dois novos pacotes. Validado localmente com instalações limpas/builds.
- Nota Windows: não executar `npm ci` da PWA enquanto o Vite estiver ativo, pois o processo pode bloquear `esbuild.exe`; encerrar o servidor antes.
- O `tsconfig.json` raiz da PWA agora também declara `strict` e `forceConsistentCasingInFileNames`; o build continuou verde.
- Publicação concluída no commit `e99c5a2` em `origin/main`; ele contém somente os artefatos UI/PWA e padrões GitHub. Alterações pré-existentes em `cli/` e `extensions/idd-core/` foram preservadas fora do commit.
- Configuração remota aplicada: Discussions, auto-merge, delete branch on merge, vulnerability alerts, Dependabot security updates, secret scanning/push protection e proteção de `main` (PR, 1 aprovação, checks, histórico linear, resolução de conversas, bloqueio de force push/deleção).
- Correção de integração em andamento: a UI publicada em `packages/` não era carregada pelo produto, que embarca apenas `extensions/idd-core`. Um `IntentWorkspacePanel` operacional foi adicionado ao `idd-core`, conectado ao `IntentStore` real e acessível pelo título e itens da Sidebar. `npm ci --ignore-scripts; npm run compile` passou no pacote embarcado.
- Integração final publicada no commit `55855b5`: `idd-core` agora expõe `IntentWorkspacePanel` ligado ao `IntentStore` real por comando e Sidebar. O pacote embarcado compilou com `npm run compile`; os arquivos preexistentes em `cli/` e `IntentEngine.ts` continuam fora do commit.
- UI integrada e publicada: `55855b5` adiciona `IntentWorkspacePanel` operacional ao único pacote embarcado (`extensions/idd-core`); `2486420` corrige o empacotamento com ícone, licença, `.vscodeignore` e tipos VS Code fixados em 1.85.0.
- Distribuição validada: `npm run compile` e `vsce package` geraram `dist/idd-core-0.1.0.vsix` (18 arquivos, 41 KB). O VSIX foi instalado no VS Code local; recarregar uma janela com workspace e abrir o container IDD/Command Palette para usar `IDD: Abrir Intent Workspace`.
- Integração operacional com LSP concluída no commit `c26f54f`: `activate()` agora inicia o cliente LSP e `deactivate()` o encerra. O VSIX foi reconstruído e reinstalado localmente após essa correção.
- Webview conectada ao workspace real no commit `b58d835`: `IntentWorkspaceScanner.ts` usa `findFiles('**/*.intent.yaml')` + `project.intent.yaml`, parseia via js-yaml e o painel publica `INTENTS_LOADED`. A árvore agrupa por Bounded Context e exibe invariantes/critérios de aceite; watcher `**/*.intent.yaml` refaz o scan.
- Schemas reais: contrato = `intent`/`module`(ctx/nome)/`constraints`/`acceptance`/`language`; `project.intent.yaml` = `bounded_contexts[].status` (usado para o status do contexto).
- Editor conectado no commit `5df7f70`: LSP agora inclui `.ts`, usa TypeScript Compiler API para detectar mutações fora de `state_mutation.allowed_fields`, publica CodeLens em `@intent(...)` e a Webview abre contrato/implementação na linha via `openFile`. Build e VSIX passaram; VSIX reinstalado localmente.
- Avaliação arquitetural 2026-09-04: média qualitativa aproximada de 62%; forte em contratos/schema, store, static drift e testes CLI; parcial em geração integrada, domínio formal, LSP/extension e governança; fraco em semântica fail-closed e ciclo único de autoridade. CLI validado com 24 suítes/841 testes passando e build verde.
- Ajustes P0 publicados em `c4677c4`: schema/validador/documentação agora aceitam `state_mutation.allowed_fields`; `verify --semantic` retorna `unknown` em ausência/erro do provider e `--semantic-required` falha CI; CLI explicita `inconclusivo`. Testes passaram com 843 casos.
- Parecer + proposta em `docs/PARECER-E-PROPOSTA-2026-09-04.md`. Cruzou repo × Stockley (6 elementos, risk dials) × Eliezer (epistêmico/operacional, DAV, confirm-once, genealogia) × Spec IDD-IDE v1.0/Blueprint v2.0 (3 camadas, L1/SHALA, circunscrição target_class.target_method, Git bimodal, hard/soft gates, Unisys SIM). Achado-chave: "fork do Code-OSS" é só declarativo — `product.json`/docs afirmam, mas o repo não contém Code-OSS nem pipeline gulp/electron. Proposta: um núcleo `@idd/core` com dois alvos — Alvo A extensão VS Code (subconjunto) e Alvo B IDE via `ide/vscode` submodule + 6 patches mínimos (built-in, Explorer intencional, Sombra read-only, Welcome intent, SCM bimodal, branding). Fases 0–6 com gates.
- Fontes externas do paradigma: `C:\IDE IDD Claude\` (Spec v1.0, Blueprint v2.0 Gemini, Intent Spec visual VLM) e `C:\Antigravity - RVM Designações\IDD Paradigma\` (knowledge-base, manifesto, Ep01–03 roteiros/vídeos, backlog Ep04–08).
- Veredito às contra-propostas em `docs/VEREDITO-CONTRAPROPOSTAS-2026-09-04.md`. CP1 Gherkin puro → ACEITAR COM AJUSTE: `.intent.md` com caminho `<ctx>/<Classe>.<metodo>.intent.md` como circunscrição, frontmatter só `authorization`+`lifecycle_min_phase`, corpo LN + blocos gherkin; compilador falha se frontmatter duplicar chave do YAML. CP2 branch `shadow/main` → ACEITAR (branch protection remota é o único enforcement real; hooks locais são cooperação). CP3 dicionário manual antes de SHALA → ACEITAR (DAV Layer 0). R1 waiver expira por fase 30/14/7d (Blueprint fixa 7d prod). R2 anemic model detector → ACEITAR. R3 README por patch → ACEITAR. R4 fidelity sparkline + dial evidence-gated → ACEITAR. Sequência: contrato v2 → dicionário → track+shadow → AST → governança → HUD → SHALA → fork → visual.
- **RATIFICADO por Eliezer ("todas ok", 2026-09-04)**: §5 do veredito — (1) `<ctx>/<Classe>.<metodo>.intent.md`; (2) frontmatter só `authorization`+`lifecycle_min_phase`; (3) `shadow/main` + tags `shadow/v*`; (4) waiver 30/14/7d por fase; (5) anemic model warn/critical/critical; (6) dial relaxa com ≥95% em 50 verificações. Confirm-once concedido para executar §2 do veredito. Fase 0 iniciada.
- Erro de comando ausente diagnosticado pela cópia instalada/log do Extension Host: a extensão abortava antes de `registerCommand` por `Cannot find module 'uuid'`, pois o VSIX excluía `node_modules` mas carregava JS não bundleado. Corrigido e publicado em `8673233`: `main` usa `dist/extension.js`; host e LSP são bundles esbuild autocontidos; CI e `scripts/build.sh` executam `npm run build`. VSIX final contém 9 arquivos e foi reinstalado localmente.
- **Fase 0 CONCLUÍDA localmente (2026-09-05)** — commits locais `811c9f9` (perf: prompt caching `cache_control` em todos os system prompts) e `d923f51` (feat core). Conteúdo: `packages/core` (`@idd/core`) com `contract.ts` (parser único v1+v2) e `project.ts` (`parseProject`, `DEFAULT_PHASE_POLICIES` 30/14/7 + warn/critical/critical, governance, `extractImports`/`contextForPath`/`checkFileImports`); CLI e LSP consomem via dependência `file:` (NÃO usar `paths`/alias vitest — quebra no Windows); `idd verify --project` varre `bounded_contexts[].path` e sai 1 em importação ilegal; `project.intent.yaml` na raiz governa o próprio repo (5 contextos: core, cli→core, extension→core, ui-extension, pwa; 88 arquivos, 0 violações); `schemas/project.intent.schema.json`; CI `idd-verify.yml` roda `verify --project` como hard gate; extensão tsconfig `noEmit` (esbuild é dono de `dist/`). Testes: 25 suítes / 877 passando. Gate de saída da Fase 0 atendido (1 parser; v1 válido; importação ilegal bloqueada).
- **PUSHED** `811c9f9`+`d923f51` (Fase 0) e `0611645` (item #2) para `origin/main` em 2026-09-05. Push direto em main continua funcionando para o owner.
- **Item #2 CONCLUÍDO — Dicionário Ubíquo (DAV Layer 0), commit `0611645`**: `packages/core/src/dictionary.ts` (`parseDictionary`, `findTerm`, `checkContractTerms` → warnings `unknown` p/ tokens PascalCase ausentes e `forbidden` p/ sinônimos proibidos; sem LLM); `cli/src/commands/dictionary.ts` (`idd dictionary init|list|show|add|remove|check [--strict]`, `loadDictionary`/`saveDictionary`/`checkIntentAgainstDictionary`); `capture` avisa antes de gravar; `verify` trata como warn (nunca drift); LSP publica `idd.dictionary.unknown|forbidden` (Warning, posicionado no termo, cache por mtime, busca ascendente por `.intent/ubiquitous-dictionary.json`). Dogfooding: `.intent/ubiquitous-dictionary.json` com 18 termos do próprio IDD (o check achou `FileChange` e `GraphIssue` indefinidos → definidos via CLI). CI: `dictionary check --strict` como hard gate. Testes: 26 suítes / 901. Gate de saída atendido (termo fora do dicionário gera warning offline).

## ONDE ESTAMOS (salvo 2026-09-06 a pedido de Eliezer)
- **HEAD = `9957a20` = origin/main.** `a4c3bad` = docs (ROADMAP Fase 9 + SESSAO-2026-09-05); `9957a20` = espelho da memória do agente em `docs/memoria-agente/{repo,session,user}` + README com origens no disco e script de re-sync. **Convenção nova: ao fim de cada bloco, além de atualizar a memória, re-copiar para `docs/memoria-agente/`** (script no README). Working tree limpo. Remoto tem 2 branches Dependabot novas (`types/node-26.4.0`, `typescript-7.0.2` em packages/vscode-extension) — não tratadas.
- **Docs do projeto refletem o estado**: `docs/ROADMAP.md` ganhou "Fase 9 — Paradigma IDD-IDE" com tabela dos 9 itens do veredito §2 (1–2 ✅, 3 ⏳) + números (901 testes / 26 suítes / 25 comandos); `docs/SESSAO-2026-09-05-FASE0-DICIONARIO.md` registra decisões, entregas, o que os gates revelaram, validação e onde a conversa parou.
- **Concluídos do veredito §2:** #1 Fase 0 (contrato v2 + `@idd/core` + `verify --project`) e #2 Dicionário Ubíquo. Detalhe técnico em `/memories/repo/fase0-e-dicionario-ubiquo-2026-09-05.md`.
- **Estágio da conversa:** eu havia proposto iniciar #3 (Fase 1) e levantado 1 decisão epistêmica: branch protection remota de `shadow/main` — aplicar via `gh api` se houver `gh` autenticado, senão deixar comando pronto para Eliezer. Eliezer respondeu pedindo para salvar o estado (este registro). **Fase 1 NÃO iniciada.** Ao retomar: confirmar "prossiga" → começar #3 (ver plano no arquivo de snapshot).
- **Convenções operacionais em vigor:** confirm-once concedido para todo §2; push direto em `main` aceito; commits em inglês conventional (`feat/fix/perf/docs`), corpo com bullets; testes/tsc/build da extensão verdes antes de cada commit; memória atualizada antes de prosseguir a cada bloco.



